import styled from 'styled-components';

const AddAttributeWrapper = styled.div`
  box-sizing: border-box;
  padding: 12px;
  width: ${(props) => props.width || '292px'};
  background-color: white;
`;

export default AddAttributeWrapper;
