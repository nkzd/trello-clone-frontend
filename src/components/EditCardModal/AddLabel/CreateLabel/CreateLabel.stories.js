import React from 'react';
import CreateLabel from './index';

export default {
  title: 'Modal/CreateLabel',
  component: CreateLabel,
};

const Template = (args) => <CreateLabel {...args} />;

export const Default = Template.bind({});
Default.args = {
};