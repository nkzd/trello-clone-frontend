import React from 'react';
import AddAnotherList from './index';

export default {
  title: 'AddAnotherList',
  component: AddAnotherList,
};

const Template = (args) => <AddAnotherList {...args} />;

export const Default = Template.bind({});
