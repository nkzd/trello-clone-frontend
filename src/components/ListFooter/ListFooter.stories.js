import React from 'react';
import ListFooter from './index';

export default {
  title: 'ListFooter',
  component: ListFooter,
};

const Template = (args) => <ListFooter {...args} />;

export const Default = Template.bind({});
