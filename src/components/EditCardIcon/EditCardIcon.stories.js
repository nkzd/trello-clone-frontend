import React from 'react';
import EditCardIcon from './index';

export default {
  title: 'EditCardIcon',
  component: EditCardIcon,
};

const Template = (args) => <EditCardIcon {...args} />;

export const Primary = Template.bind({});